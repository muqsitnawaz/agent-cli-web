#!/usr/bin/env bash
# test.sh — full test suite.
set -euo pipefail
cd "$(dirname "$0")/.."

# No automated tests yet. When tests are added, replace this line:
#   bun test
echo "[test] no tests configured yet — placeholder"
